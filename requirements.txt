# Core runtime dependencies for the AI Refund Support Agent.
#
# The system runs out of the box on these alone, using the dependency-free
# deterministic heuristic LLM provider. The optional Ollama provider additionally
# uses `httpx` (listed here) to talk to a local Ollama server; no API keys are used.

pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
httpx>=0.27,<1.0        # only needed for the optional local Ollama provider
rich>=13.7,<14.0        # nicer CLI output (the CLI degrades gracefully without it)

# --- Optional HTTP API (bonus). Uncomment to enable the FastAPI server. ---
# fastapi>=0.110,<1.0
# uvicorn[standard]>=0.29,<1.0
