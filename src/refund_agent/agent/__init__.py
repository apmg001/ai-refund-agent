"""Agent package.

Contains the function-calling agent loop, the tool abstraction and registry, and
the prompt templates that wire the deterministic refund services to a (pluggable)
LLM provider.
"""

from __future__ import annotations

__all__: list[str] = []
